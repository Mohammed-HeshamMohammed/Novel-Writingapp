{
  "name": "novel-writingapp-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Electron desktop shell for Novel-Writingapp",
  "main": "main.cjs",
  "scripts": {
    "electron": "electron .",
    "start-dev": "concurrently \"npm --prefix .. run dev\" \"wait-on http://localhost:5173 && npm run electron\"",
    "build": "npm --prefix .. run build && npm run electron"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "electron": "^37.2.1",
    "wait-on": "^8.0.3"
  }
}
